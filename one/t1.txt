Что-то там какой-то стих.

Читать файл.  - Модуль чтения файла. Уже установлен, подключаем fs
way 1

fs.readFile(файл, кодировка,функция получающая и обрабатывающая результат (err,data =>{console.log(data)}))

Нод Асинхронен, он не ждёт выполнение программы
